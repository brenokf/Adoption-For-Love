import React from 'react';
import { StyledCheckbox } from './Checkbox.style';

const Checkbox = () => (
  <StyledCheckbox data-testid="Checkbox">
    <h1>Checkbox component</h1>
  </StyledCheckbox>
);

export default Checkbox;
